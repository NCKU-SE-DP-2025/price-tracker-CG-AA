repos:
  - repo: https://github.com/psf/black
    rev: 24.8.0   # pin a version
    hooks:
      - id: black

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix]  # fixes import order (I), whitespace, etc. per PEP 8
